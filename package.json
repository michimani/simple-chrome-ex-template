{
  "name": "simple-chrome-ex-template",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "fmt": "npx @biomejs/biome format --write ./src",
		"fmt:check": "npx @biomejs/biome format ./src",
		"lint": "npx @biomejs/biome lint ./src"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/chrome": "^0.0.287",
    "typescript": "~5.6.2",
    "vite": "^6.0.9"
  }
}
